<div class="divider relative @isset($class){{$class}}@endisset">
    @isset($title)
        <div class="title absolute w-full text-sm font-medium justify-center flex -top-2">
            <p class="DLL text-zinc-400 px-2 rounded-md">{{$title}}</p>
        </div>
    @endisset
</div>
